// Supabase Infrastructure Exports
export { createClient as createBrowserClient } from './browser'
export type { Database, Json } from './types'

// Server client removed - Vite SPA projects only use browser client
// Archived files available in docs/archived/ for reference

